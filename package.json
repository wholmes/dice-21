{
  "name": "poker-dice",
  "private": true,
  "author": "Whittfield Holmes",
  "type": "module",
  "scripts": {
    "dj:manifest": "node scripts/generate-dj-manifest.mjs",
    "predev": "npm run dj:manifest",
    "prebuild": "npm run dj:manifest",
    "dev": "vite",
    "build": "vite build",
    "build:arcade": "BASE_PATH=/arcade/ vite build",
    "preview": "vite preview",
    "preview:arcade": "BASE_PATH=/arcade/ vite preview",
    "mp-server": "node mp-server.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
